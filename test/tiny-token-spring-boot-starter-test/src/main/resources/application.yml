spring:
  devtools:
    restart:
      enabled: true
  servlet:
    multipart:
      max-file-size: 4GB
      max-request-size: 4GB

tiny-token:
  header: Authorization
  username: root
  password: admin

logging:
  level:
    root: info
  file:
    name: /log/tiny-token-spring-boot-starter-test.log
